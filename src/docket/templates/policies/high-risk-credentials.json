{
  "id": "high-risk-credentials",
  "class": "high-risk",
  "description": "Secret/credential write operations always require approval (high-risk class — overrides allowlist)",
  "applies_to": ["*"],
  "hook": "pre_tool_call",
  "match": {
    "type": "regex",
    "pattern": "vault\\s+(write|kv\\s+put)|kubectl\\s+(create|apply).*secret|aws.*secretsmanager.*put-secret|openssl\\s+genrsa|ssh-keygen|gpg.*gen-key|adduser\\b|useradd\\b|usermod\\b|chpasswd\\b|passwd\\s+\\w"
  },
  "action": "require_approval",
  "message": "Secret/credential write always requires approval, regardless of exec allowlist."
}
