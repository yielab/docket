# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
*.swp
*.swo
*~
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Temporary files
*.tmp
*.bak
*.backup
tmp/
temp/

# Logs
logs/
*.log

# Test artifacts
test-*-agent/
.test-*/

# Smoke-test harness: holds the real-name anonymization map + captures of the
# live fleet. Published demo assets live in docs/assets/ (curated/anonymized).
smoke-test/

# docket's own runtime state, if ever created inside the repo (normally under
# ~/.docket/, outside the tree entirely -- this only guards a DOCKET_HOME
# override pointed in-repo, e.g. for local testing)
.docket/

# Internal documentation
internal-docs/
AUDIT_REPORT.md
CRITICAL_ISSUES.md
TERMINAL-MODE-QUICKSTART.txt

# Local development
.env
.env.local
.secrets

# Claude artifacts
.claude/
# AI assistant dev guidance (kept local, not published)
CLAUDE.md
__pycache__/
.venv/

# Dependency-floors check (CI job `floors`; see the "Running Tests" recipe in
# CLAUDE.md) and release-build artifacts (`.github/workflows/release.yml`),
# if ever run locally from the repo root instead of a scratch dir.
.floorenv/
floors.txt
dist/
